{
  "name": "clabs-cli",
  "version": "1.0.0",
  "description": "CLI tool for create components, pages, hooks and others templates",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsc --watch",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rodrigomoyano11",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.6.4"
  }
}
