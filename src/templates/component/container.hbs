import { {{pascalCase name}}Component } from './{{camelCase name}}.component'

export type {{pascalCase name}}ContainerProps = {
  children: React.ReactNode
}

export const {{pascalCase name}}Container = (props: {{pascalCase name}}ContainerProps) => <{{pascalCase name}}Component {...props} />
