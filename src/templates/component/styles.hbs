import css from 'styled-jsx/css'

export const {{pascalCase name}}LocalStyles = css`
  .box {
    background-color: var(--color);
    padding: 4rem;
  }
`

export const {{pascalCase name}}GlobalStyles = css.global`
  div {
    font-family: Arial, Helvetica, sans-serif;
  }
`
